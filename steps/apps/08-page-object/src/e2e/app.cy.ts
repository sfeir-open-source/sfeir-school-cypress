import { getGreeting } from '../support/app.po';

describe('react-solution', () => {
  beforeEach(() => {
    cy.visit('/');
  });

  it('should add an item to the cart', () => {
    // TODO: instanciate the CartPage object
    // TODO: add to cart an element with the CartPage instance
  });
});
